class Result {
  int rotationCount(int a[], int size)
  {
    // Write your code here
    int count = 0;
        for (int i = 0; i < size-1; i++)
        {
            if(a[i]>a[i+1]){
              count = i+1;
            }
        }
    return count;   
  }
}
